---
meta:
  environment: python
  toolchain:
    name: pip
    version: 3.13
  port: 80
build:
  requirementsPath: requirements.txt
  useCache: true
run:
  persistenceMount: /data
  command: uvicorn bot:app --host 0.0.0.0 --port 80 --proxy-headers --forwarded-allow-ips='*'

